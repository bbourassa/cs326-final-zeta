// this is temporary
// a more secure secret will later be generated and stored as an environment variable
// remember to add .env to .gitignore once we do that

exports.secret = 'thequickbrownfox';
exports.expireTime = '1h';
